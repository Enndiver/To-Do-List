#To-Do List App

A modern Python task manager with due dates, color-coded priorities, and a clean purple UI. Built with Tkinter as a personal learning project. Run `pip install tkcalendar` before using.
